{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "rootDir": "./", // Now test and src can both be compiled
        "noEmit": true // No output needed during testing
    },
    "include": [
        "src/**/*",
        "test/**/*"
    ]
}